# 任務管理系統專案說明文件

## 📋 專案概述

這是一個基於 **React** 前端和 **Django** 後端的全端任務管理系統，旨在幫助使用者有效管理日常任務，提升工作效率。本專案採用前後端分離架構，提供完整的 RESTful API 介面，並具備現代化的使用者介面。

---

## 🎯 專案目標

- 學習前後端分離架構的開發方式
- 掌握 React 前端框架的核心概念
- 熟悉 Django REST Framework 的 API 開發
- 理解使用者認證與授權機制
- 實踐資料庫設計與關聯
- 培養全端開發能力
- **養成版本控制習慣**：每完成一個開發階段就推送到 GitHub

---

## ✨ 功能特色

### 1. 使用者認證系統
- **用戶註冊**：新用戶可以建立帳號
- **用戶登入**：使用 JWT Token 進行身份驗證
- **密碼重設**：忘記密碼時可透過郵件重設
- **個人資料管理**：編輯個人資訊、修改密碼

### 2. 任務管理核心功能
- **建立任務**：新增任務標題、描述、截止日期
- **查看任務**：以列表或卡片形式瀏覽所有任務
- **編輯任務**：修改任務內容、狀態、優先級等
- **刪除任務**：移除不需要的任務
- **任務詳情**：查看完整的任務資訊

### 3. 任務分類與組織
- **分類管理**：建立自訂分類（工作、學習、生活等）
- **標籤系統**：為任務添加多個標籤
- **優先級設定**：高、中、低三種優先級
- **狀態管理**：待辦、進行中、已完成、已取消

### 4. 搜尋與篩選
- **關鍵字搜尋**：快速找到特定任務
- **狀態篩選**：依任務狀態過濾
- **優先級篩選**：依優先級過濾
- **分類篩選**：依分類過濾
- **日期篩選**：依截止日期範圍過濾
- **組合篩選**：多條件組合查詢

### 5. 時間管理
- **截止日期**：設定任務完成期限
- **日期提醒**：過期任務自動標示
- **今日任務**：快速查看今日需完成的任務
- **本週任務**：查看本週任務概覽
- **時間戳記**：記錄建立與更新時間

### 6. 資料統計（進階功能）
- **完成率統計**：計算任務完成百分比
- **分類統計**：各分類的任務數量分布
- **時間分布**：任務在不同時間段的分布情況
- **完成趨勢**：任務完成率的趨勢圖表

---

## 🛠️ 技術棧

### 前端技術
- **React 18+**：現代化的前端框架
- **React Router**：單頁應用路由管理
- **Axios**：HTTP 請求處理
- **Context API / Redux**：狀態管理
- **Material-UI / Ant Design**：UI 元件庫（可選）
- **React Hook Form**：表單處理與驗證
- **Date-fns**：日期處理工具

### 後端技術
- **Django 4.x**：Python Web 框架
- **Django REST Framework**：RESTful API 開發
- **Django CORS Headers**：跨域請求處理
- **JWT (djangorestframework-simplejwt)**：Token 認證
- **PostgreSQL / SQLite**：資料庫（開發階段可用 SQLite）
- **Django Filter**：進階過濾功能

### 開發工具
- **Git**：版本控制
- **Postman / Insomnia**：API 測試工具
- **ESLint / Prettier**：程式碼格式化
- **Black**：Python 程式碼格式化

### 容器化與部署
- **Docker**：應用程式容器化
- **Docker Compose**：多容器應用編排
- **Nginx**：反向代理與靜態檔案服務（生產環境）

---

## 📁 專案結構

```
task-management-system/
├── backend/                    # Django 後端專案
│   ├── manage.py
│   ├── requirements.txt
│   ├── backend/               # Django 專案設定
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── accounts/              # 用戶認證應用
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── serializers.py
│   │   └── urls.py
│   ├── tasks/                 # 任務管理應用
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── serializers.py
│   │   ├── filters.py
│   │   └── urls.py
│   └── categories/            # 分類管理應用
│       ├── models.py
│       ├── views.py
│       ├── serializers.py
│       └── urls.py
│
├── frontend/                   # React 前端專案
│   ├── package.json
│   ├── Dockerfile             # 前端 Docker 設定
│   ├── .dockerignore
│   ├── nginx.conf             # Nginx 設定（生產環境）
│   ├── public/
│   └── src/
│       ├── components/        # React 元件
│       │   ├── TaskList.jsx
│       │   ├── TaskForm.jsx
│       │   ├── TaskCard.jsx
│       │   └── FilterBar.jsx
│       ├── pages/             # 頁面元件
│       │   ├── Login.jsx
│       │   ├── Register.jsx
│       │   ├── Dashboard.jsx
│       │   └── TaskDetail.jsx
│       ├── services/          # API 服務
│       │   └── api.js
│       ├── context/           # Context API
│       │   └── AuthContext.jsx
│       ├── utils/             # 工具函數
│       │   └── auth.js
│       └── App.jsx
│
├── docker-compose.yml         # Docker Compose 設定
├── .env.example               # 環境變數範例
└── README.md                  # 專案說明文件
```

---

## 🗄️ 資料庫設計

### 用戶模型 (User)
- `id`：主鍵
- `username`：用戶名稱（唯一）
- `email`：電子郵件（唯一）
- `password`：密碼（加密儲存）
- `first_name`：名字
- `last_name`：姓氏
- `date_joined`：註冊時間
- `last_login`：最後登入時間

### 分類模型 (Category)
- `id`：主鍵
- `name`：分類名稱
- `color`：分類顏色（用於 UI 顯示）
- `user`：外鍵關聯到 User（每個用戶有自己的分類）
- `created_at`：建立時間

### 任務模型 (Task)
- `id`：主鍵
- `title`：任務標題
- `description`：任務描述
- `status`：任務狀態（待辦/進行中/已完成/已取消）
- `priority`：優先級（高/中/低）
- `due_date`：截止日期
- `user`：外鍵關聯到 User
- `category`：外鍵關聯到 Category（可選）
- `created_at`：建立時間
- `updated_at`：更新時間
- `completed_at`：完成時間（可選）

### 標籤模型 (Tag) - 選用
- `id`：主鍵
- `name`：標籤名稱
- `user`：外鍵關聯到 User
- `tasks`：多對多關聯到 Task

---

## 🚀 實作過程

### 階段一：環境設定與專案初始化

#### 1.1 後端環境設定
```bash
# 建立虛擬環境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安裝 Django 及相關套件
pip install django djangorestframework
pip install djangorestframework-simplejwt
pip install django-cors-headers
pip install django-filter

# 建立 Django 專案
django-admin startproject backend
cd backend
python manage.py startapp accounts
python manage.py startapp tasks
python manage.py startapp categories
```

#### 1.2 前端環境設定
```bash
# 使用 Create React App 建立專案
npx create-react-app frontend
cd frontend

# 安裝必要套件
npm install axios react-router-dom
npm install @mui/material @emotion/react @emotion/styled  # 選用 Material-UI
npm install react-hook-form
npm install date-fns
```

#### 1.3 設定 CORS
在 Django 的 `settings.py` 中設定允許前端跨域請求：
```python
INSTALLED_APPS = [
    ...
    'corsheaders',
    'rest_framework',
    'rest_framework_simplejwt',
]

MIDDLEWARE = [
    'corsheaders.middleware.CorsMiddleware',
    ...
]

CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",  # React 開發伺服器
]
```

> ✅ **階段一完成檢查點**：
> - [ ] 後端環境設定完成
> - [ ] 前端環境設定完成
> - [ ] CORS 設定完成
> - [ ] **推送到 GitHub**：`git add . && git commit -m "feat: 完成階段一 - 環境設定與專案初始化" && git push`

---

### 階段二：後端 API 開發

#### 2.1 資料模型設計
在 `tasks/models.py` 中定義任務模型：
```python
from django.db import models
from django.contrib.auth.models import User

class Task(models.Model):
    STATUS_CHOICES = [
        ('pending', '待辦'),
        ('in_progress', '進行中'),
        ('completed', '已完成'),
        ('cancelled', '已取消'),
    ]
    
    PRIORITY_CHOICES = [
        ('high', '高'),
        ('medium', '中'),
        ('low', '低'),
    ]
    
    title = models.CharField(max_length=200)
    description = models.TextField(blank=True)
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='pending')
    priority = models.CharField(max_length=10, choices=PRIORITY_CHOICES, default='medium')
    due_date = models.DateTimeField(null=True, blank=True)
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    category = models.ForeignKey('categories.Category', on_delete=models.SET_NULL, null=True, blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    completed_at = models.DateTimeField(null=True, blank=True)
```

#### 2.2 Serializer 開發
在 `tasks/serializers.py` 中建立序列化器：
```python
from rest_framework import serializers
from .models import Task

class TaskSerializer(serializers.ModelSerializer):
    class Meta:
        model = Task
        fields = ['id', 'title', 'description', 'status', 'priority', 
                  'due_date', 'category', 'created_at', 'updated_at', 'completed_at']
        read_only_fields = ['id', 'created_at', 'updated_at']
```

#### 2.3 View 開發
在 `tasks/views.py` 中建立視圖：
```python
from rest_framework import viewsets, filters
from rest_framework.permissions import IsAuthenticated
from django_filters.rest_framework import DjangoFilterBackend
from .models import Task
from .serializers import TaskSerializer

class TaskViewSet(viewsets.ModelViewSet):
    serializer_class = TaskSerializer
    permission_classes = [IsAuthenticated]
    filter_backends = [DjangoFilterBackend, filters.SearchFilter, filters.OrderingFilter]
    filterset_fields = ['status', 'priority', 'category']
    search_fields = ['title', 'description']
    ordering_fields = ['created_at', 'due_date', 'priority']
    ordering = ['-created_at']
    
    def get_queryset(self):
        return Task.objects.filter(user=self.request.user)
    
    def perform_create(self, serializer):
        serializer.save(user=self.request.user)
```

#### 2.4 URL 路由設定
在 `tasks/urls.py` 中設定路由：
```python
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from .views import TaskViewSet

router = DefaultRouter()
router.register(r'tasks', TaskViewSet)

urlpatterns = [
    path('', include(router.urls)),
]
```

#### 2.5 JWT 認證設定
在 `backend/settings.py` 中設定 JWT：
```python
from datetime import timedelta

SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(hours=1),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),
}
```

> ✅ **階段二完成檢查點**：
> - [ ] 資料模型設計完成
> - [ ] Serializer 開發完成
> - [ ] ViewSet 與 API 端點完成
> - [ ] JWT 認證設定完成
> - [ ] **推送到 GitHub**：`git add . && git commit -m "feat: 完成階段二 - 後端 API 開發" && git push`

---

### 階段三：前端開發

#### 3.1 API 服務層
在 `src/services/api.js` 中建立 API 呼叫函數：
```javascript
import axios from 'axios';

const API_URL = 'http://localhost:8000/api';

const api = axios.create({
  baseURL: API_URL,
});

// 請求攔截器：自動添加 Token
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export const taskAPI = {
  getAll: () => api.get('/tasks/'),
  getById: (id) => api.get(`/tasks/${id}/`),
  create: (data) => api.post('/tasks/', data),
  update: (id, data) => api.put(`/tasks/${id}/`, data),
  delete: (id) => api.delete(`/tasks/${id}/`),
};

export const authAPI = {
  login: (credentials) => api.post('/auth/login/', credentials),
  register: (data) => api.post('/auth/register/', data),
  logout: () => {
    localStorage.removeItem('access_token');
    localStorage.removeItem('refresh_token');
  },
};
```

#### 3.2 認證 Context
在 `src/context/AuthContext.jsx` 中建立認證狀態管理：
```javascript
import React, { createContext, useState, useContext, useEffect } from 'react';
import { authAPI } from '../services/api';

const AuthContext = createContext();

export const useAuth = () => {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within AuthProvider');
  }
  return context;
};

export const AuthProvider = ({ children }) => {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    // 檢查本地儲存的 Token
    const token = localStorage.getItem('access_token');
    if (token) {
      // 驗證 Token 並獲取用戶資訊
      // 實作驗證邏輯
    }
    setLoading(false);
  }, []);

  const login = async (credentials) => {
    const response = await authAPI.login(credentials);
    localStorage.setItem('access_token', response.data.access);
    localStorage.setItem('refresh_token', response.data.refresh);
    setUser(response.data.user);
  };

  const logout = () => {
    authAPI.logout();
    setUser(null);
  };

  return (
    <AuthContext.Provider value={{ user, login, logout, loading }}>
      {children}
    </AuthContext.Provider>
  );
};
```

#### 3.3 任務列表元件
在 `src/components/TaskList.jsx` 中建立任務列表：
```javascript
import React, { useState, useEffect } from 'react';
import { taskAPI } from '../services/api';
import TaskCard from './TaskCard';

const TaskList = () => {
  const [tasks, setTasks] = useState([]);
  const [loading, setLoading] = useState(true);
  const [filters, setFilters] = useState({
    status: '',
    priority: '',
    search: '',
  });

  useEffect(() => {
    fetchTasks();
  }, [filters]);

  const fetchTasks = async () => {
    try {
      const params = {};
      if (filters.status) params.status = filters.status;
      if (filters.priority) params.priority = filters.priority;
      if (filters.search) params.search = filters.search;
      
      const response = await taskAPI.getAll({ params });
      setTasks(response.data);
    } catch (error) {
      console.error('獲取任務失敗:', error);
    } finally {
      setLoading(false);
    }
  };

  const handleDelete = async (id) => {
    try {
      await taskAPI.delete(id);
      setTasks(tasks.filter(task => task.id !== id));
    } catch (error) {
      console.error('刪除任務失敗:', error);
    }
  };

  if (loading) return <div>載入中...</div>;

  return (
    <div>
      <FilterBar filters={filters} setFilters={setFilters} />
      <div className="task-list">
        {tasks.map(task => (
          <TaskCard
            key={task.id}
            task={task}
            onDelete={handleDelete}
          />
        ))}
      </div>
    </div>
  );
};

export default TaskList;
```

> ✅ **階段三完成檢查點**：
> - [ ] API 服務層建立完成
> - [ ] 認證 Context 實作完成
> - [ ] 主要頁面元件開發完成
> - [ ] **推送到 GitHub**：`git add . && git commit -m "feat: 完成階段三 - 前端開發" && git push`

---

### 階段四：功能整合與測試

#### 4.1 前後端連線測試
- 使用 Postman 測試所有 API 端點
- 確認 CORS 設定正確
- 驗證 JWT Token 認證流程

#### 4.2 功能測試
- 用戶註冊與登入流程
- 任務的 CRUD 操作
- 搜尋與篩選功能
- 錯誤處理與驗證

#### 4.3 UI/UX 優化
- 響應式設計調整
- 載入狀態顯示
- 錯誤訊息提示
- 成功操作回饋

> ✅ **階段四完成檢查點**：
> - [ ] 前後端連線測試通過
> - [ ] 功能完整性測試通過
> - [ ] UI/UX 優化完成
> - [ ] **推送到 GitHub**：`git add . && git commit -m "feat: 完成階段四 - 功能整合與測試" && git push`

---

### 階段五：Docker 容器化部署

#### 5.1 Docker 簡介

**什麼是 Docker？**
Docker 是一個開源的容器化平台，可以將應用程式及其依賴項打包成輕量級的容器，確保應用程式在任何環境中都能一致運行。

**為什麼使用 Docker？**
- **環境一致性**：開發、測試、生產環境完全一致
- **快速部署**：一鍵啟動整個應用系統
- **資源隔離**：每個容器獨立運行，互不干擾
- **易於擴展**：可以輕鬆擴展服務實例
- **簡化依賴管理**：不需要在本地安裝所有依賴

**Docker Compose 是什麼？**
Docker Compose 是用於定義和運行多容器 Docker 應用的工具，通過一個 YAML 檔案配置所有服務。

---

#### 5.2 後端 Dockerfile 設定

在 `backend/` 目錄下建立 `Dockerfile`：

```dockerfile
# 使用官方 Python 3.11 作為基礎映像
FROM python:3.11-slim

# 設定工作目錄
WORKDIR /app

# 設定環境變數
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# 安裝系統依賴
RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        postgresql-client \
        build-essential \
    && rm -rf /var/lib/apt/lists/*

# 複製 requirements 檔案
COPY requirements.txt .

# 安裝 Python 依賴
RUN pip install --no-cache-dir -r requirements.txt

# 複製專案檔案
COPY . .

# 建立靜態檔案目錄
RUN mkdir -p /app/staticfiles

# 暴露埠號
EXPOSE 8000

# 執行啟動腳本
CMD ["gunicorn", "backend.wsgi:application", "--bind", "0.0.0.0:8000"]
```

在 `backend/` 目錄下建立 `.dockerignore`：

```
__pycache__
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.git
.mypy_cache
.pytest_cache
.hypothesis
db.sqlite3
staticfiles/
media/
.env
```

更新 `backend/requirements.txt`，加入生產環境需要的套件：

```txt
Django==4.2.7
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1
django-filter==23.5
psycopg2-binary==2.9.9
gunicorn==21.2.0
python-decouple==3.8
```

---

#### 5.3 前端 Dockerfile 設定

在 `frontend/` 目錄下建立 `Dockerfile`：

```dockerfile
# 第一階段：建置 React 應用
FROM node:18-alpine as build

WORKDIR /app

# 複製 package 檔案
COPY package*.json ./

# 安裝依賴
RUN npm ci

# 複製專案檔案
COPY . .

# 建置應用（生產版本）
RUN npm run build

# 第二階段：使用 Nginx 提供靜態檔案服務
FROM nginx:alpine

# 複製建置好的檔案到 Nginx
COPY --from=build /app/build /usr/share/nginx/html

# 複製 Nginx 設定檔
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露埠號
EXPOSE 80

# 啟動 Nginx
CMD ["nginx", "-g", "daemon off;"]
```

在 `frontend/` 目錄下建立 `.dockerignore`：

```
node_modules
npm-debug.log
build
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.git
.gitignore
README.md
```

在 `frontend/` 目錄下建立 `nginx.conf`：

```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip 壓縮
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;

    # React Router 支援（處理前端路由）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 靜態資源快取
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API 代理（可選：如果需要透過 Nginx 代理 API）
    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

#### 5.4 Docker Compose 設定

在專案根目錄建立 `docker-compose.yml`：

```yaml
version: '3.8'

services:
  # PostgreSQL 資料庫服務
  db:
    image: postgres:15-alpine
    container_name: task_management_db
    environment:
      POSTGRES_DB: ${DB_NAME:-taskdb}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - task_network

  # Django 後端服務
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: task_management_backend
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             gunicorn backend.wsgi:application --bind 0.0.0.0:8000"
    volumes:
      - ./backend:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DEBUG=${DEBUG:-False}
      - DATABASE_URL=postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@db:5432/${DB_NAME:-taskdb}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - task_network
    restart: unless-stopped

  # React 前端服務
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - REACT_APP_API_URL=${REACT_APP_API_URL:-http://localhost:8000}
    container_name: task_management_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - task_network
    restart: unless-stopped

volumes:
  postgres_data:
  static_volume:
  media_volume:

networks:
  task_network:
    driver: bridge
```

---

#### 5.5 環境變數設定

在專案根目錄建立 `.env.example`：

```env
# Django 設定
DEBUG=False
SECRET_KEY=your-secret-key-here-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# 資料庫設定
DB_NAME=taskdb
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# JWT 設定
JWT_ACCESS_TOKEN_LIFETIME=3600
JWT_REFRESH_TOKEN_LIFETIME=604800

# CORS 設定
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80

# React 設定
REACT_APP_API_URL=http://localhost:8000
```

建立實際的 `.env` 檔案（不要提交到 Git）：

```bash
cp .env.example .env
# 然後編輯 .env 檔案，修改敏感資訊
```

更新 `backend/backend/settings.py` 以支援環境變數：

```python
from decouple import config
import os

# 基本設定
SECRET_KEY = config('SECRET_KEY', default='django-insecure-change-this-in-production')
DEBUG = config('DEBUG', default=False, cast=bool)
ALLOWED_HOSTS = config('ALLOWED_HOSTS', default='localhost,127.0.0.1').split(',')

# 資料庫設定
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': config('DB_NAME', default='taskdb'),
        'USER': config('DB_USER', default='postgres'),
        'PASSWORD': config('DB_PASSWORD', default='postgres'),
        'HOST': config('DB_HOST', default='db'),
        'PORT': config('DB_PORT', default='5432'),
    }
}

# CORS 設定
CORS_ALLOWED_ORIGINS = config(
    'CORS_ALLOWED_ORIGINS',
    default='http://localhost:3000'
).split(',')
```

更新 `frontend/src/services/api.js` 以使用環境變數：

```javascript
const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:8000/api';
```

---

#### 5.6 Docker 部署步驟

**步驟 1：安裝 Docker 和 Docker Compose**

- **Windows/Mac**：下載並安裝 [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux**：
```bash
# 安裝 Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# 安裝 Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

**步驟 2：準備環境變數**

```bash
# 複製環境變數範例檔案
cp .env.example .env

# 編輯 .env 檔案，設定你的配置
# 注意：SECRET_KEY 和 DB_PASSWORD 要改成安全的隨機字串
```

**步驟 3：建置並啟動容器**

```bash
# 建置所有服務的映像檔
docker-compose build

# 啟動所有服務（背景執行）
docker-compose up -d

# 查看服務狀態
docker-compose ps

# 查看日誌
docker-compose logs -f

# 只查看後端日誌
docker-compose logs -f backend

# 只查看前端日誌
docker-compose logs -f frontend
```

**步驟 4：執行資料庫遷移**

```bash
# 進入後端容器執行遷移
docker-compose exec backend python manage.py migrate

# 建立超級用戶（可選）
docker-compose exec backend python manage.py createsuperuser
```

**步驟 5：訪問應用**

- **前端**：http://localhost:3000
- **後端 API**：http://localhost:8000/api
- **Django 管理後台**：http://localhost:8000/admin

**步驟 6：停止服務**

```bash
# 停止所有服務
docker-compose down

# 停止服務並刪除 volumes（會刪除資料庫資料）
docker-compose down -v
```

---

#### 5.7 常用 Docker 指令

```bash
# 查看運行中的容器
docker ps

# 查看所有容器（包含已停止）
docker ps -a

# 查看映像檔
docker images

# 進入容器內部
docker-compose exec backend bash
docker-compose exec frontend sh

# 重新建置特定服務
docker-compose build backend

# 重新啟動特定服務
docker-compose restart backend

# 查看容器資源使用情況
docker stats

# 清理未使用的資源
docker system prune

# 查看 Docker Compose 服務狀態
docker-compose ps

# 執行一次性指令（例如建立超級用戶）
docker-compose run --rm backend python manage.py createsuperuser
```

---

#### 5.8 開發模式與生產模式

**開發模式（使用 Docker Compose）**

建立 `docker-compose.dev.yml`：

```yaml
version: '3.8'

services:
  db:
    # ... 與上面相同

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend:/app  # 掛載本地程式碼，支援熱重載
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - DEBUG=True
    # ... 其他設定

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev  # 開發用 Dockerfile
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm start
    # ... 其他設定
```

使用開發模式：

```bash
docker-compose -f docker-compose.dev.yml up
```

**生產模式優化**

- 使用多階段建置減少映像檔大小
- 設定適當的資源限制
- 使用 Nginx 作為反向代理
- 啟用 HTTPS
- 設定日誌輪轉
- 使用 Docker Secrets 管理敏感資訊

---

#### 5.9 Docker 部署常見問題

**問題 1：容器無法連接到資料庫**

**解決方案**：
- 確認 `depends_on` 設定正確
- 檢查資料庫健康檢查是否通過
- 確認環境變數中的資料庫連線資訊正確
- 檢查網路設定（所有服務應在同一網路）

**問題 2：前端無法連接到後端 API**

**解決方案**：
- 確認 `REACT_APP_API_URL` 環境變數設定正確
- 檢查 CORS 設定是否允許前端來源
- 確認後端服務正常運行：`docker-compose logs backend`
- 在生產環境中，可能需要透過 Nginx 代理 API 請求

**問題 3：靜態檔案無法載入**

**解決方案**：
```bash
# 收集靜態檔案
docker-compose exec backend python manage.py collectstatic --noinput

# 確認 volumes 掛載正確
docker-compose exec backend ls -la /app/staticfiles
```

**問題 4：容器重啟後資料遺失**

**解決方案**：
- 確認使用 volumes 持久化資料庫資料
- 檢查 `docker-compose.yml` 中的 volumes 設定
- 不要使用 `docker-compose down -v`（會刪除 volumes）

**問題 5：建置時間過長**

**解決方案**：
- 使用 `.dockerignore` 排除不必要的檔案
- 利用 Docker 層快取，將不常變動的步驟放在前面
- 使用多階段建置減少最終映像檔大小

**問題 6：權限問題（Linux）**

**解決方案**：
```bash
# 確保當前用戶在 docker 群組中
sudo usermod -aG docker $USER
# 登出後重新登入
```

---

#### 5.10 Docker 學習重點

1. **容器化概念**：理解容器與虛擬機器的差異
2. **Dockerfile 撰寫**：掌握多階段建置、層快取等技巧
3. **Docker Compose**：學習多容器應用的編排
4. **網路與儲存**：理解 Docker 網路和 volumes 的使用
5. **環境變數管理**：安全地管理敏感資訊
6. **映像檔優化**：減少映像檔大小，提升建置速度
7. **生產環境部署**：學習最佳實踐和安全設定

---

#### 5.11 進階部署選項

**使用 Docker Swarm 或 Kubernetes**

對於更複雜的部署需求，可以考慮：
- **Docker Swarm**：Docker 原生的容器編排工具
- **Kubernetes**：更強大的容器編排平台，適合大規模部署

**CI/CD 整合**

- 使用 GitHub Actions、GitLab CI 等自動化建置和部署
- 自動執行測試
- 自動建置 Docker 映像檔
- 自動部署到生產環境

**監控與日誌**

- 使用 Prometheus 監控容器資源使用
- 使用 Grafana 視覺化監控資料
- 整合 ELK Stack 進行日誌管理

> ✅ **階段五完成檢查點**：
> - [ ] 後端 Dockerfile 建立完成
> - [ ] 前端 Dockerfile 建立完成
> - [ ] Docker Compose 配置完成
> - [ ] 環境變數設定完成
> - [ ] Docker 部署測試成功
> - [ ] **推送到 GitHub**：`git add . && git commit -m "feat: 完成階段五 - Docker 容器化部署" && git push`

---

### 階段六：Git 版本控制與 GitHub 部署

> ⚠️ **重要提醒**：建議在開始專案開發前就初始化 Git 儲存庫，並在**每個階段完成後**立即將程式碼推送到 GitHub。這樣可以：
> - 養成良好的版本控制習慣
> - 隨時備份程式碼，避免遺失
> - 清楚記錄開發進度
> - 方便回顧和展示專案發展歷程
> - 如果遇到問題，可以輕鬆回到上一個穩定版本

#### 6.1 Git 與 GitHub 簡介

**什麼是 Git？**
Git 是一個分散式版本控制系統，可以追蹤程式碼的變更歷史，方便團隊協作和程式碼管理。

**什麼是 GitHub？**
GitHub 是一個基於 Git 的程式碼託管平台，提供程式碼儲存、版本控制、協作開發等功能。

**為什麼要使用 Git 和 GitHub？**
- **版本控制**：追蹤所有程式碼變更，可以隨時回到之前的版本
- **團隊協作**：多人可以同時開發，透過分支和合併管理程式碼
- **備份與同步**：程式碼儲存在雲端，不會遺失
- **開源分享**：可以公開專案，讓其他人學習和貢獻
- **專案展示**：GitHub 是程式設計師的履歷，展示你的專案能力

---

#### 6.2 Git 基本指令

**初始化 Git 儲存庫**

```bash
# 在專案根目錄初始化 Git
git init

# 查看 Git 狀態
git status

# 查看變更內容
git diff
```

**基本操作指令**

```bash
# 將檔案加入暫存區
git add <檔案名稱>
git add .  # 加入所有變更的檔案

# 提交變更（建立一個版本）
git commit -m "提交訊息說明"

# 查看提交歷史
git log
git log --oneline  # 簡潔版本

# 查看分支
git branch

# 建立新分支
git branch <分支名稱>

# 切換分支
git checkout <分支名稱>
git checkout -b <分支名稱>  # 建立並切換到新分支

# 合併分支
git merge <分支名稱>

# 刪除分支
git branch -d <分支名稱>
```

**遠端儲存庫操作**

```bash
# 添加遠端儲存庫
git remote add origin <GitHub儲存庫URL>

# 查看遠端儲存庫
git remote -v

# 推送到遠端
git push -u origin main  # 第一次推送
git push  # 之後的推送

# 從遠端拉取
git pull

# 從遠端複製專案
git clone <GitHub儲存庫URL>
```

---

#### 6.3 建立 GitHub 儲存庫

**步驟 1：在 GitHub 建立新儲存庫**

1. 登入 GitHub 帳號
2. 點擊右上角的「+」號，選擇「New repository」
3. 填寫儲存庫資訊：
   - **Repository name**：`task-management-system`（或你喜歡的名稱）
   - **Description**：`A full-stack task management system built with React and Django`
   - **Visibility**：選擇 Public（公開）或 Private（私有）
   - **不要**勾選「Initialize this repository with a README」（我們已經有本地專案）
4. 點擊「Create repository」

**步驟 2：連接本地專案到 GitHub**

```bash
# 在專案根目錄執行
git init
git add .
git commit -m "Initial commit: Task Management System"

# 添加遠端儲存庫（替換成你的 GitHub 儲存庫 URL）
git remote add origin https://github.com/你的使用者名稱/task-management-system.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**步驟 3：設定 Git 使用者資訊（如果還沒設定）**

```bash
# 設定使用者名稱和電子郵件
git config --global user.name "你的名字"
git config --global user.email "你的email@example.com"

# 查看設定
git config --list
```

---

#### 6.4 建立 .gitignore 檔案

在專案根目錄建立 `.gitignore` 檔案，避免提交不必要的檔案：

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.mypy_cache
.pytest_cache
.hypothesis
*.db
*.sqlite3
db.sqlite3

# Django
*.log
local_settings.py
staticfiles/
media/
/static/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm
.eslintcache

# React
build/
dist/
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# 環境變數
.env
.env.local
.env.*.local

# Docker
.dockerignore

# 作業系統
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 其他
*.bak
*.tmp
*.temp
```

在 `backend/` 目錄建立 `.gitignore`：

```gitignore
# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
/static/
/staticfiles/
/media/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv
```

在 `frontend/` 目錄建立 `.gitignore`（如果使用 Create React App，會自動產生）：

```gitignore
# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Production
/build
/dist

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
```

---

#### 6.5 建立 README.md 檔案

在專案根目錄建立 `README.md`，這是 GitHub 會自動顯示的專案說明文件：

```markdown
# 任務管理系統 (Task Management System)

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python](https://img.shields.io/badge/Python-3.11-blue.svg)](https://www.python.org/)
[![React](https://img.shields.io/badge/React-18+-61dafb.svg)](https://react.dev/)
[![Django](https://img.shields.io/badge/Django-4.2-green.svg)](https://www.djangoproject.com/)

一個基於 React 前端和 Django 後端的全端任務管理系統，幫助使用者有效管理日常任務，提升工作效率。

## ✨ 功能特色

- 🔐 **使用者認證**：JWT Token 認證機制
- 📝 **任務管理**：完整的 CRUD 操作
- 🏷️ **分類與標籤**：靈活的任務組織方式
- 🔍 **搜尋與篩選**：快速找到需要的任務
- ⏰ **時間管理**：截止日期提醒與統計
- 📊 **資料統計**：任務完成率與趨勢分析

## 🛠️ 技術棧

### 前端
- React 18+
- React Router
- Axios
- Context API

### 後端
- Django 4.2
- Django REST Framework
- PostgreSQL
- JWT Authentication

### 部署
- Docker
- Docker Compose
- Nginx

## 🚀 快速開始

### 前置需求

- Python 3.11+
- Node.js 18+
- Docker & Docker Compose（可選）

### 使用 Docker 部署（推薦）

```bash
# 1. 複製專案
git clone https://github.com/你的使用者名稱/task-management-system.git
cd task-management-system

# 2. 設定環境變數
cp .env.example .env
# 編輯 .env 檔案，設定你的配置

# 3. 啟動服務
docker-compose up -d

# 4. 執行資料庫遷移
docker-compose exec backend python manage.py migrate

# 5. 建立超級用戶（可選）
docker-compose exec backend python manage.py createsuperuser
```

訪問應用：
- 前端：http://localhost:3000
- 後端 API：http://localhost:8000/api
- 管理後台：http://localhost:8000/admin

### 本地開發

#### 後端設定

```bash
cd backend

# 建立虛擬環境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安裝依賴
pip install -r requirements.txt

# 執行遷移
python manage.py migrate

# 啟動開發伺服器
python manage.py runserver
```

#### 前端設定

```bash
cd frontend

# 安裝依賴
npm install

# 啟動開發伺服器
npm start
```

## 📁 專案結構

```
task-management-system/
├── backend/          # Django 後端
├── frontend/         # React 前端
├── docker-compose.yml
└── README.md
```

## 📖 詳細文件

請參考 [專案說明文件.md](./專案說明文件.md) 查看完整的開發文件。

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

## 📄 授權

本專案採用 MIT 授權條款。

## 👨‍💻 作者

[您的名字]

## 🙏 致謝

感謝所有為這個專案做出貢獻的人！
```

---

#### 6.6 GitHub 最佳實踐

**提交訊息規範**

使用清晰的提交訊息，遵循以下格式：

```bash
# 格式：<類型>: <簡短描述>

# 類型：
# feat: 新功能
# fix: 修復 bug
# docs: 文件更新
# style: 程式碼格式調整（不影響功能）
# refactor: 程式碼重構
# test: 測試相關
# chore: 建置過程或輔助工具的變動

# 範例：
git commit -m "feat: 新增任務篩選功能"
git commit -m "fix: 修復登入時 Token 過期問題"
git commit -m "docs: 更新 API 文件"
git commit -m "refactor: 重構任務列表元件"
```

**分支管理策略**

```bash
# 主分支
main/master    # 生產環境程式碼，保持穩定

# 開發分支
develop        # 開發環境程式碼

# 功能分支
feature/任務篩選功能
feature/使用者認證

# 修復分支
fix/登入錯誤修復
fix/API回應問題

# 熱修復分支（緊急修復）
hotfix/安全漏洞修復
```

**工作流程範例**

```bash
# 1. 從 main 分支建立功能分支
git checkout main
git pull origin main
git checkout -b feature/新增任務功能

# 2. 開發功能並提交
git add .
git commit -m "feat: 新增任務建立表單"

# 3. 推送到遠端
git push origin feature/新增任務功能

# 4. 在 GitHub 上建立 Pull Request
# 5. 審查通過後合併到 main 分支
```

---

#### 6.7 GitHub 功能使用

**Issues（問題追蹤）**

- 用於追蹤 bug、功能請求、任務等
- 可以指派給團隊成員
- 可以加上標籤（Labels）分類
- 可以連結到 Pull Request

**Pull Request（合併請求）**

- 用於程式碼審查
- 可以討論程式碼變更
- 可以要求特定人員審查
- 通過審查後合併到主分支

**GitHub Actions（CI/CD）**

建立 `.github/workflows/ci.yml` 自動化測試和部署：

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
      - name: Run tests
        run: |
          cd backend
          python manage.py test

  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: |
          cd frontend
          npm install
      - name: Run tests
        run: |
          cd frontend
          npm test -- --coverage --watchAll=false
```

**GitHub Pages（靜態網站託管）**

可以用來部署前端應用的靜態版本：

```bash
# 在 frontend/package.json 中添加部署腳本
"scripts": {
  "deploy": "npm run build && gh-pages -d build"
}
```

---

#### 6.8 保護敏感資訊

**重要：不要提交敏感資訊到 GitHub！**

**需要保護的資訊：**
- `.env` 檔案
- 資料庫密碼
- API 金鑰
- JWT Secret Key
- 第三方服務的認證資訊

**解決方案：**

1. **使用 .gitignore**：確保 `.env` 檔案被忽略
2. **使用環境變數範例**：提供 `.env.example` 檔案
3. **使用 GitHub Secrets**：在 GitHub Actions 中使用 Secrets
4. **如果已經提交敏感資訊**：
   ```bash
   # 從 Git 歷史中移除檔案
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch .env" \
     --prune-empty --tag-name-filter cat -- --all
   
   # 強制推送（謹慎使用）
   git push origin --force --all
   ```

---

#### 6.9 專案展示優化

**README.md 優化技巧：**

1. **使用徽章（Badges）**：顯示專案狀態、技術棧等
2. **添加截圖**：展示專案實際運行畫面
3. **清晰的結構**：使用目錄和清晰的章節
4. **快速開始指南**：讓其他人能快速上手
5. **貢獻指南**：說明如何參與專案

**添加專案截圖：**

```markdown
## 📸 專案截圖

### 任務列表頁面
![任務列表](./screenshots/task-list.png)

### 任務詳情頁面
![任務詳情](./screenshots/task-detail.png)
```

在專案中建立 `screenshots/` 目錄存放截圖。

**添加專案徽章：**

可以使用 [shields.io](https://shields.io/) 產生各種徽章：

```markdown
![Python](https://img.shields.io/badge/Python-3.11-blue)
![Django](https://img.shields.io/badge/Django-4.2-green)
![React](https://img.shields.io/badge/React-18+-61dafb)
![License](https://img.shields.io/badge/License-MIT-yellow)
```

---

#### 6.10 Git 學習重點

1. **版本控制概念**：理解 commit、branch、merge 等基本概念
2. **基本指令**：add、commit、push、pull、clone
3. **分支管理**：建立、切換、合併分支
4. **衝突解決**：學會處理合併衝突
5. **遠端協作**：理解 push、pull、fetch 的差異
6. **GitHub 功能**：Issues、Pull Request、Actions
7. **最佳實踐**：提交訊息規範、分支策略

---

#### 6.11 常見 Git 問題

**問題 1：忘記提交某些檔案**

```bash
# 修改最後一次提交
git add <遺漏的檔案>
git commit --amend --no-edit
```

**問題 2：提交到錯誤的分支**

```bash
# 1. 記住提交的 hash
git log

# 2. 切換到正確的分支
git checkout <正確的分支>

# 3. Cherry-pick 該提交
git cherry-pick <提交hash>

# 4. 回到原分支，重置
git checkout <原分支>
git reset --hard HEAD~1
```

**問題 3：想要撤銷本地提交**

```bash
# 撤銷最後一次提交（保留變更）
git reset --soft HEAD~1

# 撤銷最後一次提交（丟棄變更）
git reset --hard HEAD~1
```

**問題 4：合併衝突**

```bash
# 1. 查看衝突檔案
git status

# 2. 編輯衝突檔案，解決衝突標記
# <<<<<<< HEAD
# 你的程式碼
# =======
# 其他人的程式碼
# >>>>>>> branch-name

# 3. 標記為已解決
git add <解決衝突的檔案>
git commit -m "解決合併衝突"
```

**問題 5：想要更新遠端分支**

```bash
# 先拉取遠端變更
git fetch origin

# 合併遠端變更
git merge origin/main

# 或使用 rebase（保持歷史線性）
git rebase origin/main
```

> ✅ **階段六完成檢查點**：
> - [ ] Git 儲存庫初始化完成
> - [ ] .gitignore 檔案建立完成
> - [ ] README.md 檔案建立完成
> - [ ] GitHub 遠端儲存庫連接完成
> - [ ] 所有階段程式碼已推送到 GitHub
> - [ ] **最後推送**：`git add . && git commit -m "docs: 完成階段六 - Git 與 GitHub 設定" && git push`

---

#### 6.12 實習生 Git 工作流程建議

**專案階段性推送建議：**

> 📌 **關鍵原則：每完成一個開發階段，就推送到 GitHub！**

建議的推送時機：
1. ✅ **階段一完成後**：環境設定與專案初始化完成
2. ✅ **階段二完成後**：後端 API 開發完成
3. ✅ **階段三完成後**：前端開發完成
4. ✅ **階段四完成後**：功能整合與測試完成
5. ✅ **階段五完成後**：Docker 容器化部署完成
6. ✅ **階段六完成後**：Git 與 GitHub 設定完成

**每個階段的推送步驟：**

```bash
# 1. 檢查變更狀態
git status

# 2. 加入所有變更
git add .

# 3. 提交變更（使用階段名稱）
git commit -m "feat: 完成階段一 - 環境設定與專案初始化"

# 4. 推送到 GitHub
git push origin main
```

**每日工作流程：**

```bash
# 1. 開始工作前，更新本地程式碼
git checkout main
git pull origin main

# 2. 建立功能分支（如果是在開發新功能）
git checkout -b feature/今天要做的功能

# 3. 開發功能，頻繁提交
git add .
git commit -m "feat: 實作任務列表元件"

# 4. 推送到遠端
git push origin feature/今天要做的功能

# 5. 在 GitHub 建立 Pull Request（如果使用分支）

# 6. 完成一個階段後，合併到 main 並推送
git checkout main
git merge feature/今天要做的功能
git push origin main
```

**提交頻率建議：**
- ✅ **完成一個開發階段就推送一次**（最重要！）
- 完成一個小功能就提交一次
- 修復一個 bug 就提交一次
- 每天結束前提交當天的工作進度
- 不要累積太多變更才提交

**階段性提交訊息範例：**

```bash
# 階段一
git commit -m "feat: 完成階段一 - 環境設定與專案初始化"

# 階段二
git commit -m "feat: 完成階段二 - 後端 API 開發（任務 CRUD、認證）"

# 階段三
git commit -m "feat: 完成階段三 - 前端開發（任務列表、表單、認證）"

# 階段四
git commit -m "feat: 完成階段四 - 功能整合與測試"

# 階段五
git commit -m "feat: 完成階段五 - Docker 容器化部署"

# 階段六
git commit -m "docs: 完成階段六 - Git 與 GitHub 設定"
```

---

## 📝 開發檢查清單

### 後端開發
- [ ] Django 專案初始化
- [ ] 資料模型設計與遷移
- [ ] Serializer 開發
- [ ] ViewSet 與 API 端點
- [ ] JWT 認證設定
- [ ] 權限控制實作
- [ ] 過濾與搜尋功能
- [ ] API 文件生成（Swagger/OpenAPI）

### 前端開發
- [ ] React 專案初始化
- [ ] 路由設定
- [ ] API 服務層建立
- [ ] 認證 Context 實作
- [ ] 登入/註冊頁面
- [ ] 任務列表頁面
- [ ] 任務表單元件
- [ ] 篩選與搜尋功能
- [ ] 錯誤處理

### 整合與部署
- [ ] 前後端連線測試
- [ ] 功能完整性測試
- [ ] 效能優化
- [ ] 安全性檢查
- [ ] Docker 容器化設定
- [ ] Dockerfile 撰寫（前端與後端）
- [ ] Docker Compose 配置
- [ ] 環境變數管理
- [ ] 生產環境部署測試

### Git 與 GitHub
- [ ] 初始化 Git 儲存庫
- [ ] 建立 .gitignore 檔案
- [ ] 建立 README.md 檔案
- [ ] 在 GitHub 建立遠端儲存庫
- [ ] 推送程式碼到 GitHub
- [ ] 設定分支保護規則
- [ ] 建立 Issues 追蹤問題
- [ ] 使用 Pull Request 進行程式碼審查
- [ ] 設定 GitHub Actions CI/CD（可選）

---

## 🎓 學習重點

### 後端學習重點
1. **Django 模型設計**：理解資料庫關聯與遷移
2. **RESTful API 設計**：掌握 REST 架構原則
3. **認證與授權**：JWT Token 機制實作
4. **資料過濾**：Django Filter 的使用
5. **序列化器**：資料驗證與轉換

### 前端學習重點
1. **React Hooks**：useState, useEffect, useContext
2. **狀態管理**：Context API 或 Redux
3. **路由管理**：React Router 的使用
4. **HTTP 請求**：Axios 的封裝與攔截器
5. **表單處理**：React Hook Form 的使用
6. **元件設計**：可重用元件的開發

### Docker 與部署學習重點
1. **容器化概念**：理解 Docker 的基本原理
2. **Dockerfile 撰寫**：掌握映像檔建置技巧
3. **Docker Compose**：多容器應用編排
4. **環境變數管理**：安全地管理配置資訊
5. **網路與儲存**：Docker 網路和 volumes 的使用
6. **生產環境部署**：學習最佳實踐和優化技巧

### Git 與 GitHub 學習重點
1. **版本控制概念**：理解 commit、branch、merge 等基本概念
2. **基本指令**：add、commit、push、pull、clone
3. **分支管理**：建立、切換、合併分支
4. **衝突解決**：學會處理合併衝突
5. **遠端協作**：理解 push、pull、fetch 的差異
6. **GitHub 功能**：Issues、Pull Request、Actions
7. **最佳實踐**：提交訊息規範、分支策略

---

## 🔧 常見問題與解決方案

### 問題 1：CORS 錯誤
**解決方案**：確認 Django 的 `CORS_ALLOWED_ORIGINS` 設定正確，包含前端開發伺服器的 URL。

### 問題 2：JWT Token 過期
**解決方案**：實作 Token 自動刷新機制，使用 Refresh Token 獲取新的 Access Token。

### 問題 3：資料驗證錯誤
**解決方案**：在前後端都實作完整的資料驗證，提供清晰的錯誤訊息。

### 問題 4：效能問題
**解決方案**：使用分頁功能，避免一次載入過多資料；實作資料快取機制。

### 問題 5：Docker 容器無法啟動
**解決方案**：檢查日誌 `docker-compose logs`，確認環境變數設定正確，檢查埠號是否被占用。

### 問題 6：資料庫連線失敗
**解決方案**：確認資料庫服務已啟動並通過健康檢查，檢查 `DATABASE_URL` 環境變數，確認網路設定正確。

### 問題 7：Git 推送被拒絕
**解決方案**：先執行 `git pull` 拉取遠端變更，解決衝突後再推送。如果是第一次推送，確認遠端儲存庫 URL 正確。

### 問題 8：不小心提交了敏感資訊
**解決方案**：立即從 Git 歷史中移除該檔案，並更改所有相關的密碼和金鑰。參考階段六的「保護敏感資訊」章節。

---

## 📚 參考資源

- [Django 官方文件](https://docs.djangoproject.com/)
- [Django REST Framework 文件](https://www.django-rest-framework.org/)
- [React 官方文件](https://react.dev/)
- [React Router 文件](https://reactrouter.com/)
- [JWT 認證指南](https://jwt.io/)
- [Docker 官方文件](https://docs.docker.com/)
- [Docker Compose 文件](https://docs.docker.com/compose/)
- [Docker 最佳實踐](https://docs.docker.com/develop/dev-best-practices/)
- [Git 官方文件](https://git-scm.com/doc)
- [GitHub 文件](https://docs.github.com/)
- [Git 教學 - 連猴子都能懂的 Git 入門指南](https://backlog.com/git-tutorial/tw/)
- [Pro Git 中文版](https://git-scm.com/book/zh-tw/v2)

---

## 🎯 未來擴展方向

1. **即時通知**：使用 WebSocket 實作任務提醒
2. **檔案上傳**：為任務添加附件功能
3. **協作功能**：任務指派與團隊協作
4. **資料視覺化**：使用 Chart.js 或 D3.js 建立統計圖表
5. **行動應用**：使用 React Native 開發行動版
6. **單元測試**：為前後端添加完整的測試覆蓋
7. **CI/CD 整合**：使用 GitHub Actions 或 GitLab CI 自動化部署
8. **容器編排**：使用 Kubernetes 進行大規模部署
9. **監控與日誌**：整合 Prometheus、Grafana 等監控工具
10. **負載平衡**：使用 Nginx 或 HAProxy 進行負載分散

---

## 📄 授權說明

本專案為教學用途，可自由使用與修改。

---

**最後更新日期**：2024年

**專案維護者**：[您的名字]

